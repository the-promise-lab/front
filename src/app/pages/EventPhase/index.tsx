import { IconMusic, IconStar } from '@shared/ui/icons';

export default function EventPhase() {
  return (
    <div className='h-screen w-screen bg-[#000021]'>
      <h1>EventPhase</h1>
      <IconStar />
      <IconMusic size={64} />
    </div>
  );
}
